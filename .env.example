# Copy to .env for local runs. .env is gitignored; never commit real secrets.
# In production these are set as Portainer stack environment variables.

# --- Server (read today by internal/config) ---

# TCP port the server listens on.
PORT=8080

# slog level: debug | info | warn | error
LOG_LEVEL=info

# Public origin of this instance. Used for OIDC redirects and absolute links.
BASE_URL=http://localhost:8080

# --- Deployment only (docker-compose.yml, not read by the binary) ---

# Public domain Traefik routes to this stack, e.g. robocolony.example.com
HOSTNAME=localhost

# Name of the external Traefik docker network.
TRAEFIK_NETWORK_NAME=traefik_default

# --- Declared now, consumed from E2 onward (nothing reads these yet) ---

# Path to the SQLite database file. Inside the container this lives on the
# ./data volume, which must be writable by uid/gid 1000.
DB_PATH=./data/robocolony.db

# Random 32+ byte secret used to sign session cookies.
# Generate with: openssl rand -base64 48
SESSION_SECRET=

# Google OAuth 2.0 / OIDC credentials from the Google Cloud console.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Must exactly match an authorised redirect URI on the OAuth client.
GOOGLE_REDIRECT_URL=http://localhost:8080/auth/google/callback
